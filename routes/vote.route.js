const api = require("express").Router();
// const { validToken } = require("../middlewares/auth");
// const {
//   validValue,
//   validUser,
//   validBook,
// } = require("../middlewares/vote.middleware");
// const { vote } = require("../controllers/vote.controller");

// api.patch("/", validToken, validValue, validUser, validBook, vote);
module.exports = api;
